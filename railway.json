{
  "$schema": "https://railway.com/railway.schema.json",
  "//": "One service, one image. The PWA, the API and the background worker all run in this container as a single Bun process. Do not scale numReplicas above 1: the worker loop is single-instance and two copies would both claim the same pending deletion job.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/Dockerfile",
    "watchPatterns": ["**"]
  },
  "deploy": {
    "healthcheckPath": "/health/live",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "numReplicas": 1
  }
}
